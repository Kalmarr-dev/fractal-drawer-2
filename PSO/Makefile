# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  PSO_config = debug

else ifeq ($(config),release)
  PSO_config = release

else ifeq ($(config),emscriptenrelease)
  PSO_config = emscriptenrelease

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := PSO

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

PSO:
ifneq (,$(PSO_config))
	@echo "==== Building PSO ($(PSO_config)) ===="
	@${MAKE} --no-print-directory -C build -f Makefile config=$(PSO_config)
endif

clean:
	@${MAKE} --no-print-directory -C build -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  emscriptenrelease"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   PSO"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"