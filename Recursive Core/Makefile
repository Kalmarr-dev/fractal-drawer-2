# GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  RecursiveCore_config = debug

else ifeq ($(config),release)
  RecursiveCore_config = release

else ifeq ($(config),emscriptenrelease)
  RecursiveCore_config = emscriptenrelease

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := RecursiveCore

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

RecursiveCore:
ifneq (,$(RecursiveCore_config))
	@echo "==== Building RecursiveCore ($(RecursiveCore_config)) ===="
	@${MAKE} --no-print-directory -C build -f Makefile config=$(RecursiveCore_config)
endif

clean:
	@${MAKE} --no-print-directory -C build -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  emscriptenrelease"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   RecursiveCore"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"