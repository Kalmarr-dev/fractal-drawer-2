// #include "KDTree2D.h"

// template<typename T>
// KDTree2D<T>::KDTree2D() {

// }

// template<typename T>
// bool KDTree2D<T>::shape_is_inside(Position<T> corner1, Position<T> corner2, IShape<T>* p_shape) {
  
// }

// template<typename T>
// void KDTree2D<T>::add_shapes(Shapes<T> shapes) {

//   for (auto &&shape : shapes.get_shapes())
//   {
//     KDTreeNode<T>* tree = &nodes[0];
    
//     for (int i = 0; i < 2; i ++)
//       if (tree->x[i] < tree->t) // t - медіана
//         tree = tree->left;
//       else
//         tree = tree->right;
//   }
  

// }

